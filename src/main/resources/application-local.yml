agile-runner:
  agent-runtime:
    enabled: true
    datasource:
      url: jdbc:h2:file:${user.home}/.agile-runner/agent-runtime/agile-runner;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
      driver-class-name: org.h2.Driver
      username: sa
      password:

spring:
  h2:
    console:
      enabled: true
      path: /h2-console
