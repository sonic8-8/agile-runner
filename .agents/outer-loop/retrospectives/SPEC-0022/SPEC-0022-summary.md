---
spec_id: SPEC-0022
summary_status: completed
generated_at: 2026-04-12T12:20:00+09:00
task_ids:
  - TASK-0001
  - TASK-0002
  - TASK-0003
  - TASK-0004
---

# Spec 요약

## 범위 요약
- `SPEC-0022 운영용 조회 응답 문서와 기준 파일 생성 규칙 정리`는 운영용 조회 응답 가이드, 기준 파일, 자동 검증 테스트를 어떤 기준과 어떤 순서로 함께 유지할지 문서로 고정하는 데 집중했다.
- 이번 spec은 새 endpoint나 응답 필드 변경 없이, 기준 파일 이름 규칙, 파일 단위 기준, 갱신 절차, 대표 실제 앱 검증과의 경계를 정리하는 작업이었다.

## 완료된 Task
- `TASK-0001` 기준 파일 생성 규칙 안전망 확인
- `TASK-0002` 기준 파일 이름과 파일 단위 규칙 정리
- `TASK-0003` 기준 파일 갱신 절차와 대표 검증 경계 정리
- `TASK-0004` 규칙 문서 마감과 정합성 확인

## 주요 결과
- [manual-rerun-response-guide.md](/home/seaung13/workspace/agile-runner/docs/manual-rerun-response-guide.md) 에서 새 작업자가 기준 파일 이름 규칙, 파일 단위 기준, 갱신 순서, 대표 실제 앱 검증과의 경계를 한 문서에서 읽을 수 있게 됐다.
- 기준 파일은 문서 설명에 맞춰 남겨 두는 예시 응답이고, 대표 실제 앱 검증 값은 evidence와 retrospective에 남긴다는 경계를 분명히 했다.
- 가이드 앞부분에 `처음 수정할 때 빠른 순서`를 추가해, 문서를 처음 읽는 작업자도 어디서부터 따라가야 하는지 바로 보이게 했다.

## 검증 요약
- targeted test: 통과
- full `cleanTest test`: 통과
- actual app/H2 대표 검증: 이번 spec 비대상
- linked issues `#84`, `#85`, `#86`, `#87`: 모두 `CLOSED`

## 이번 spec에서 얻은 점
- 예시와 fixture를 자동 검증으로 묶는 것만으로는 충분하지 않고, 새 작업자가 어떤 순서로 가이드와 기준 파일, 테스트를 함께 고쳐야 하는지 문서로 바로 읽혀야 유지 비용이 낮아진다.
- 대표 실제 앱 검증과 문서용 기준 파일의 경계를 분리하지 않으면, 실제 실행 값과 문서 예시 값의 역할이 섞여 drift 관리가 다시 어려워질 수 있다.
- 문서 규칙 spec은 기능 추가보다 용어와 진입 순서를 분명히 적는 쪽이 효과가 크다.

## 남은 위험
- 현재 기준 파일 생성과 갱신 규칙은 문서로 정리됐지만, seed 데이터 준비 규칙은 아직 별도 spec으로 남아 있다.
- 이후 seed 데이터를 다루는 작업에서 문서 규칙과 대표 검증 준비 절차를 다시 섞으면 기준 파일 유지 비용이 올라갈 수 있다.

## 다음 spec 후보
- `SPEC-0023 운영용 조회 응답 예시 seed 데이터 정리`
